//! Shared layout constants (only the cross-component ones; sizes local to a single
//! component stay literal there).

/// Height of the bottom status-bar strip.
pub const STATUS_BAR_H: f32 = 26.0;

/// Fixed method-badge column width so request names align in lists.
pub const BADGE_W: f32 = 44.0;

/// Height of the active-tab underline.
pub const UNDERLINE_H: f32 = 2.0;
